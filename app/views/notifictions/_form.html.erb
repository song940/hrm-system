<%= form_for(@notifiction) do |f| %>
  <% if @notifiction.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@notifiction.errors.count, "error") %> prohibited this notifiction from being saved:</h2>

      <ul>
      <% @notifiction.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %>
    <%= f.collection_select :user_id,Employee.all,:id,:name %>
  </div>
  <div class="field">
    <%= f.label :msg %><br>
    <%= f.text_area :msg %>
  </div>
  <div class="actions">
    <%= f.submit :class => 'btn' %>
  </div>
<% end %>
